# M0 — Stroboscopic Yin-Yang (sample run)

> ⚠️ **SPOILER — sample workflow run, not an answer key.** This folder is *one* example
> of taking the *Stroboscopic Yin-Yang* exercise through the full TSCG workflow
> (Proposition → Analysis → Modeling → Simulation). It exists to **illustrate the
> workflow end to end** — not to hand you the "correct" answer. TSCG has no single right
> model: the head chef owns the semantic decisions, so another modeller could make
> different, equally valid choices. If you want the practice, read the statement in the
> parent folder (`StroboscopicYinYang.md`) and build your own model first.

*Author: Echopraxium with the collaboration of Claude AI*

---

**This README is a placeholder until the run is done.** The workflow run will fill in the
model's rationale below and produce, beside this file:

- `M0_StroboscopicYinYang.jsonld` — the instance ontology
- `static/` — the p5.js simulation (spinning yin-yang, speed + strobe controls)

## Model rationale
*(to be written during the Modeling step — ASFID/REVOI scoring, GenericConcepts
mobilised, the Map/Territory reading, and any M2/domain findings)*

## Convention
Exercise statements stay open in the catalogue and are **never** published with an
answer beside them. This `workflow_run_sample/` folder holds an illustrative run —
never framed as the definitive solution. Only out-of-catalogue poclets are published as
normal contributions.
