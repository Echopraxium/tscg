TscgPocletMiner/
├── main.js              ← Process principal, IPC handlers
├── preload.js           ← contextBridge (bridge Node ↔ Renderer)
├── package.json
├── renderer/
│   ├── index.html       ← UI complète (CSS Grid, charte TSCG)
│   └── renderer.js      ← Contrôleur UI (~400 lignes)
└── src/
    ├── llm/             ← 3 backends + factory
    ├── rag/             ← RAG à la volée (TF-IDF pur JS)
    └── tscg/            ← 5 prompts + orchestrateur pipeline