# Script: follow test 4 (IsSolid(0,0)) and SumTiles return
# Breakpoint on F_CALL SumTiles
b 0x1448
# Step into SumTiles
s
# Breakpoint on return from SumTiles  
b 0x144C
# Continue to breakpoint
c
# Show registers (R0 should be 9 = SumTiles result)
r
# Continue to exit
c
