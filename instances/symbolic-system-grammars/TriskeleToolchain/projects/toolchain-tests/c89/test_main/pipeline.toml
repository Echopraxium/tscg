# projects/test_main/pipeline.toml
# Author: Echopraxium with the collaboration of Claude AI
# Pipeline validation test — exit code 31 (0x1F) = all 5 tests passed
name    = "test_main"
type    = "c"
sources = ["test_main.c"]
entry   = "main"
libs    = ["tsk-libc.tvml"]
sdl2    = false

[validation]
expected_exit = 31
tests = [
    "ScaleDiv(10,2) == 5",
    "ScaleDiv(7,0)  == 0",
    "IsSolid(3,5)   == 1",
    "IsSolid(0,0)   == 0",
    "SumTiles(0,0,8,8) == 9",
]
