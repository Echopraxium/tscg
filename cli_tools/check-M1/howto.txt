# Valider tous les fichiers M1
python check_M1.py

# Dry-run (lecture seule)
python check_M1.py --dry-run

# Fichier unique
python check_M1.py --file M1_Geology.jsonld

# + Validation SHACL
python check_M1.py --shacl

# + Rapport texte
python check_M1.py --report

# CI/CD (sans couleurs + exit code)
python check_M1.py --no-color --shacl --report