
  - id: WS-9
    title: Archaeological poclets re-evaluation
    status: scoped
    priority: 12                # after WS-10 (namespace harmonisation) settles conventions
    note: >
      The 3 first poclets (+ Transistor) and the generation template predate much of the
      Layer Cake's evolution (structural grammar, canonical REVOI, Gs monoid, M0_Common).
      Re-run each through the instance pipeline (Proposition→Analysis→Modeling→Simulation)
      with the anti-overfitting guard — repair debt, do NOT re-fit the model to the mature
      framework. Distinct from WS-10: WS-10 fixes namespace convention mechanically; WS-9
      re-models semantically. A poclet like FireTriangle passes through both.
    items:
      - { id: AP-1, title: "FireTriangle", status: todo,
          note: "eagle_eye:Attractor as colon-named term -> M3_GenesisGrammar.jsonld#eagle_eye:Attractor (CTX-5 + double-colon malformed IRI + xsd:float coercion); orphan m0:mean removed in rebrand step 1" }
      - { id: AP-2, title: "ColorSynthesis (RGB/CMY/CMYK/HSL)", status: todo,
          note: "carries the retired tensor/ket formalism (crosses SC-9); deprecated-term residue" }
      - { id: AP-3, title: "ExposureTriangle", status: todo,
          note: "same generation; audit context/formalism debt" }
      - { id: AP-4, title: "Transistor", status: todo,
          note: "flagged by Michel as dated; audit" }
      - { id: AP-5, title: "M0_CONTEXT_TEMPLATE.json (the generation template itself)", status: todo,
          note: "the SOURCE of the archaeological debts: M3_GenesisSpace.jsonld (dead file -> GenesisGrammar), eagle_eye:Attractor colon-named terms (CTX-5 + double-colon), xsd:float coercions. Fixing the template stops new poclets being born with these debts." }
