# ============================================================
# TSCG Poclet Generator — requirements.txt
# Author: Echopraxium with the collaboration of Claude AI
# ============================================================

# --- GUI toolkit (mandatory) --------------------------------
PySide6>=6.6.0

# --- LLM backends (install only what you use) ---------------

# Anthropic Claude API
anthropic>=0.25.0

# Google Gemini (free tier via aistudio.google.com)
# google-generativeai>=0.5.0
google-genai>=1.62.0

# OpenAI-compatible endpoints (LM Studio, Jan, vLLM, etc.)
openai>=1.14.0

# Ollama: no Python package needed — uses requests (see below)
requests>=2.31.0

# --- RAG Augmentor (optional — omit if not using RAG) -------
chromadb>=0.4.24
sentence-transformers>=2.6.0


anthropic>=0.25.0
anthropic[vertex]>=0.25.0

# --- Transitive dependencies (auto-installed) ---------------
# numpy          (required by sentence-transformers)
# torch          (required by sentence-transformers, CPU-only is fine)
# tokenizers     (required by sentence-transformers)
# httpx          (required by anthropic)
# pydantic       (required by chromadb)
